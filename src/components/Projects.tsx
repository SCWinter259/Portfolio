import { Typography, Box } from "@mui/material";

const Projects = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        My Projects
      </Typography>
      <Typography>
        Here are some cool projects I’ve built using React, TypeScript, and
        more.
      </Typography>
    </Box>
  );
};

export default Projects;
